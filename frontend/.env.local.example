# For local development — copy to .env.local
# In production (Docker), leave NEXT_PUBLIC_API_URL empty (same-origin via Caddy)
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_APP_NAME=CleverChat
